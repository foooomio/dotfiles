[core]
	excludesfile = ~/.gitignore
	editor = vim
[filter "media"]
	clean = git-media-clean %f
	smudge = git-media-smudge %f
[push]
	default = simple
[init]
	templatedir = ~/.git_template
[alias]
	co = checkout
